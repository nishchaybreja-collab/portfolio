name: cache-test

on:
  push:

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-69ef9dc

      - name: prime-cache-69ef9dc
        run: echo "Cache hit = ${{ steps.cache.outputs.cache-hit }}"
